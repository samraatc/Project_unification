apiVersion: v1
kind: Service
metadata:
  name: {{ include "unified-accounting.fullname" . }}
  labels: {{- include "unified-accounting.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector: {{- include "unified-accounting.selectorLabels" . | nindent 4 }}
