replicaCount: 1

image:
  tag: dev-latest

autoscaling:
  enabled: false

resources:
  requests: { cpu: 100m, memory: 256Mi }
  limits: { cpu: 500m, memory: 512Mi }

ingress:
  host: admin.dev.unified.example.com
